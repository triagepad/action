// Domain types + the machine output contract (fixes.json v1).
// This module is the transplantable heart of the product: no fs, no env,
// no local paths — attachment paths are opaque keys resolved by the caller.
export {};
